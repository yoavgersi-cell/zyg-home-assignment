# Growth Intelligence Agent — reasoning layer
# With a key set, /agent generates briefs with a live Claude call.
# Without one, it falls back to curated demo briefs (demo never breaks).
ANTHROPIC_API_KEY=
# Optional override; defaults to claude-sonnet-5
ANTHROPIC_MODEL=
